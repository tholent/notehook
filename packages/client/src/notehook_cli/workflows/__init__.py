"""notehook workflows: event log + (later) runner for the automation system.

Phase 2 scope: the event log (`events.py`) and its wiring into the sync
engine. See docs/workflow-spec.md and docs/workflow-implementation-plan.md.
"""
