---
ScheduleTaskGroupItem:
  type: object
  properties:
    taskListId:
      type: string
    userId:
      type: integer
      format: int64
    title:
      type: string
    lastModified:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    isDeleted:
      type: string
    createTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64

ScheduleRecurTaskItem:
  type: object
  properties:
    taskId:
      type: string
    recurrenceId:
      type: string
    taskListId:
      type: string
    userId:
      type: integer
      format: int64
    lastModified:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    dueTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    completedTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    status:
      description: Task status string either 'needsAction' or 'completed'
      type: string
    isDeleted:
      type: string
    sort:
      description: Sort order index
      type: integer
    sortCompleted:
      type: integer
    planerSort:
      type: integer
    allSort:
      type: integer
    allSortCompleted:
      type: integer
    sortTime:
      type: integer
      format: int64
    planerSortTime:
      type: integer
      format: int64
    allSortTime:
      type: integer
      format: int64

ScheduleTaskInfo:
  type: object
  properties:
    taskId:
      type: string
    taskListId:
      type: string
    title:
      type: string
    detail:
      type: string
    lastModified:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    recurrence:
      type: string
    isReminderOn:
      description: "Whether the reminder is enabled. 'Y' for yes, 'N' for no."
      type: string
    status:
      description: Task status string
      type: string
    importance:
      description: Task importance level
      type: string
    dueTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    completedTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    links:
      type: string
      description: Base64 encoded json description of a link to a document with fields 'appName', 'fileId', 'path', "page', 'pageId'
    isDeleted:
      type: string
    sort:
      description: Sort order index
      type: integer
    sortCompleted:
      type: integer
    planerSort:
      type: integer
    allSort:
      type: integer
    allSortCompleted:
      type: integer
    sortTime:
      type: integer
      format: int64
    planerSortTime:
      type: integer
      format: int64
    allSortTime:
      type: integer
      format: int64
    scheduleRecurTask:
      type: array
      items:
        $ref: "#/ScheduleRecurTaskItem"

AddScheduleTaskGroupDTO:
  type: object
  required:
    - title
  properties:
    taskListId:
      type: string
    title:
      type: string
    lastModified:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    createTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64

UpdateScheduleTaskGroupDTO:
  type: object
  required:
    - taskListId
    - title
  properties:
    taskListId:
      type: string
    title:
      type: string
    lastModified:
      description: Timestamp in milliseconds
      type: integer
      format: int64

ClearScheduleTaskGroupDTO:
  type: object
  required:
    - taskListId
    - lastModified
  properties:
    taskListId:
      type: string
    lastModified:
      description: Timestamp in milliseconds
      type: integer
      format: int64

ScheduleTaskGroupDTO:
  type: object
  properties:
    maxResults:
      type: string
    pageToken:
      type: string

AddScheduleTaskDTO:
  type: object
  required:
    - title
  properties:
    taskId:
      type: string
    taskListId:
      type: string
    recurrenceId:
      type: string
    title:
      type: string
    detail:
      type: string
    lastModified:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    recurrence:
      type: string
    isReminderOn:
      description: "Whether the reminder is enabled. 'Y' for yes, 'N' for no."
      type: string
    status:
      description: Task status string
      type: string
    importance:
      description: Task importance level
      type: string
    dueTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    completedTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    links:
      type: string
    isDeleted:
      type: string
    sort:
      description: Sort order index
      type: integer
    sortCompleted:
      type: integer
    planerSort:
      type: integer
    allSort:
      type: integer
    allSortCompleted:
      type: integer
    sortTime:
      type: integer
      format: int64
    planerSortTime:
      type: integer
      format: int64
    allSortTime:
      type: integer
      format: int64

UpdateScheduleTaskDTO:
  type: object
  required:
    - taskId
    - title
    - lastModified
  properties:
    taskId:
      type: string
    taskListId:
      type: string
    recurrenceId:
      type: string
    title:
      type: string
    detail:
      type: string
    lastModified:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    recurrence:
      type: string
    isReminderOn:
      description: "Whether the reminder is enabled. 'Y' for yes, 'N' for no."
      type: string
    status:
      description: Task status string
      type: string
    importance:
      description: Task importance level
      type: string
    dueTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    completedTime:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    links:
      type: string
    isDeleted:
      type: string
    sort:
      description: Sort order index
      type: integer
    sortCompleted:
      type: integer
    planerSort:
      type: integer
    allSort:
      type: integer
    allSortCompleted:
      type: integer
    sortTime:
      type: integer
      format: int64
    planerSortTime:
      type: integer
      format: int64
    allSortTime:
      type: integer
      format: int64

UpdateScheduleTaskListDTO:
  type: object
  properties:
    taskListId:
      type: string
    updateScheduleTaskList:
      type: array
      items:
        $ref: "#/UpdateScheduleTaskDTO"

ScheduleTaskDTO:
  type: object
  properties:
    maxResults:
      type: string
    nextPageTokens:
      type: string
    nextSyncToken:
      type: integer
      format: int64

ScheduleSortDTO:
  type: object
  properties:
    taskListId:
      type: string
    title:
      type: string
    lastModify:
      description: Timestamp in milliseconds
      type: integer
      format: int64
    content:
      type: string

GetScheduleSortDTO:
  type: object
  properties:
    nextIndexNumber:
      type: integer

AddScheduleTaskGroupVO:
  type: object
  allOf:
    - $ref: "common.yaml#/BaseVO"
    - type: object
      properties:
        taskListId:
          type: string

GetScheduleTaskGroupVO:
  type: object
  allOf:
    - $ref: "common.yaml#/BaseVO"
    - type: object
      properties:
        taskListId:
          type: string
        userId:
          type: integer
          format: int64
        title:
          type: string
        lastModified:
          description: Timestamp in milliseconds
          type: integer
          format: int64
        isDeleted:
          type: string
        createTime:
          description: Timestamp in milliseconds
          type: integer
          format: int64

ScheduleTaskGroupVO:
  type: object
  allOf:
    - $ref: "common.yaml#/BaseVO"
    - type: object
      properties:
        pageToken:
          type: string
        scheduleTaskGroup:
          type: array
          items:
            $ref: "#/ScheduleTaskGroupItem"

AddScheduleTaskVO:
  type: object
  allOf:
    - $ref: "common.yaml#/BaseVO"
    - type: object
      properties:
        taskId:
          type: string

UpdateScheduleTaskVO:
  type: object
  allOf:
    - $ref: "common.yaml#/BaseVO"
    - type: object
      properties:
        taskId:
          type: string

ScheduleTaskVO:
  type: object
  allOf:
    - $ref: "common.yaml#/BaseVO"
    - type: object
      properties:
        taskId:
          type: integer
          format: int64
        taskListId:
          type: integer
          format: int64
        title:
          type: string
        detail:
          type: string
        lastModified:
          description: Timestamp in milliseconds
          type: integer
          format: int64
        recurrence:
          type: string
        isReminderOn:
          description: "Whether the reminder is enabled. 'Y' for yes, 'N' for no."
          type: string
        status:
          description: Task status string
          type: string
        importance:
          description: Task importance level
          type: string
        dueTime:
          description: Timestamp in milliseconds
          type: integer
          format: int64
        completedTime:
          description: Timestamp in milliseconds
          type: integer
          format: int64
        links:
          type: string
        isDeleted:
          type: string
        sort:
          description: Sort order index
          type: integer
        sortCompleted:
          type: integer
        planerSort:
          type: integer
        allSort:
          type: integer
        allSortCompleted:
          type: integer
        sortTime:
          type: integer
          format: int64
        planerSortTime:
          type: integer
          format: int64
        allSortTime:
          type: integer
          format: int64
        scheduleRecurTask:
          type: array
          items:
            $ref: "#/ScheduleRecurTaskItem"

ScheduleTaskAllVO:
  type: object
  allOf:
    - $ref: "common.yaml#/BaseVO"
    - type: object
      properties:
        nextPageToken:
          type: string
        nextSyncToken:
          type: integer
          format: int64
        scheduleTask:
          type: array
          items:
            $ref: "#/ScheduleTaskInfo"

GetScheduleSortVO:
  type: object
  allOf:
    - $ref: "common.yaml#/BaseVO"
    - type: object
      properties:
        taskListId:
          type: string
        title:
          type: string
        lastModify:
          description: Timestamp in milliseconds
          type: integer
          format: int64
        content:
          type: string
        nextIndexNumber:
          type: integer
